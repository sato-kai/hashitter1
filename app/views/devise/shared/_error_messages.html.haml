- if resource.errors.any?
  #error_explanation
    %h2.error-count
      = I18n.t("errors.messages.not_saved",                 |
        count: resource.errors.count,                       |
        resource: resource.class.model_name.human.downcase) |
    %ul.error-messages
      - resource.errors.full_messages.each.with_index(1) do |message, i|
        %li.error-message
          %span
          = i
          %span
          ：
          = message
